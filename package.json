{
  "name": "shopify-app-changeguard",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental, offline semantic change review for Shopify app configuration files (unofficial)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "changeguard": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "node dist/cli.js"
  },
  "dependencies": {
    "@iarna/toml": "2.2.5"
  },
  "devDependencies": {
    "@types/node": "24.13.5",
    "typescript": "7.0.2"
  }
}
